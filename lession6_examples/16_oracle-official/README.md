
# Oracle Setup

## Docker container Setup

```bash
docker compose up -d
```

## DBevaer

new connecttion > oracle > enter the below deatils

```bash
DB = DCS_CORE_LOCAL
username = DCS
passwoed = Itest@123
```

or

```bash
DB = DCS_CORE_LOCAL
username = system
passwoed = Itest@123
```

## Clean Up

```bash
docker compose down -v
```